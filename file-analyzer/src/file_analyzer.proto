syntax = "proto3";

package file_analyzer;

service FileAnalyzer {
  rpc CheckForErrors (JsonFileOnGcsRequest) returns (ValidationResult) {}
}

message JsonFileOnGcsRequest {
  string pathToGcs = 1;
  string corelationId = 2;
  string userId = 3;
}

message ValidationResult {
  string result = 1;
}